# Windows on Snapdragon (ARM64). Install after requirements.txt.
#
# NOTE: use AMD64 (x64) Python for the Qualcomm AI Hub export tooling and ARM64
# Python for on-device inference. Qualcomm documents this split; mixing them is
# the single most common setup failure. See docs/DEPLOYMENT.md.

-r requirements.txt

# Hexagon NPU via the QNN Execution Provider.
onnxruntime-qnn>=1.20

# GenieX -- Qualcomm's on-device LLM/VLM runtime (Tiers 2 and 3).
geniex

# Qualcomm AI Hub: model export, quantisation, and profiling on real silicon.
qai-hub
qai-hub-models

# Optional local web UI.
fastapi>=0.115
uvicorn>=0.34
